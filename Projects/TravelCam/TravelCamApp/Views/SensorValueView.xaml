<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:TravelCamApp.Models"
             x:Class="TravelCamApp.Views.SensorValueView">

    <Border BackgroundColor="#AA000000"
            StrokeShape="RoundRectangle 8"
            Padding="12,10"
            MaximumWidthRequest="160">
        <VerticalStackLayout Spacing="4">
            <VerticalStackLayout.GestureRecognizers>
                <TapGestureRecognizer Command="{Binding OpenSettingsCommand}" />
            </VerticalStackLayout.GestureRecognizers>
            <CollectionView ItemsSource="{Binding SensorItems}"
                            SelectionMode="None">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:SensorItem">
                        <VerticalStackLayout IsVisible="{Binding IsVisible}" HeightRequest="-1">
                            <Label Text="{Binding NameAndValue}"
                                   FontSize="12"
                                   TextColor="White"
                                   LineBreakMode="NoWrap" />
                        </VerticalStackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </Border>
</ContentView>
