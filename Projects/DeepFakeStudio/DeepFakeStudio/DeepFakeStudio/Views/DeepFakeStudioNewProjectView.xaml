<UserControl
    x:Class="DeepFakeStudio.Views.DeepFakeStudioNewProjectView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:DeepFakeStudio.ViewModels"
    d:DataContext="{d:DesignInstance viewmodels:DeepFakeStudioNewProjectViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <StackPanel>
            <TextBlock Text="Source Video" />
            <TextBox Text="{Binding SourceVideoPath}" />
            <Button Command="{Binding SelectSourceVideoCommand}" Content="Load" />
            <TextBlock Text="Destination Video" />
            <TextBox Text="{Binding DestinationVideoPath}" />
            <Button Command="{Binding SelectDestinationVideoCommand}" Content="Load" />
        </StackPanel>
    </Grid>
</UserControl>