<UserControl
    x:Class="DeepFakeStudio.Views.DeepFakeStudioNewProjectView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:DeepFakeStudio.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:viewmodels="clr-namespace:DeepFakeStudio.ViewModels"
    d:DataContext="{d:DesignInstance viewmodels:DeepFakeStudioNewProjectViewModel}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <!--  Source Video  -->
        <controls:MediaElementPlayer
            Grid.Row="0"
            Grid.Column="0"
            Margin="2"
            Source="{Binding SourceVideoPath}" />
        <Grid Grid.Row="1" Grid.Column="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox
                Grid.Column="0"
                Margin="2"
                materialDesign:HintAssist.HelperText="Helper text longer than error"
                materialDesign:HintAssist.Hint="Source video path"
                Text="{Binding SourceVideoPath, UpdateSourceTrigger=PropertyChanged}" />
            <Button Grid.Column="1" Command="{Binding SelectSourceVideoCommand}">
                <materialDesign:PackIcon Kind="FolderOpen" />
            </Button>
        </Grid>
        <!--  Destination Video  -->
        <controls:MediaElementPlayer
            Grid.Row="0"
            Grid.Column="1"
            Margin="2"
            Source="{Binding DestinationVideoPath}" />
        <Grid Grid.Row="1" Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox
                Grid.Column="0"
                Margin="2"
                materialDesign:HintAssist.HelperText="Helper text longer than error"
                materialDesign:HintAssist.Hint="Source video path"
                Text="{Binding SourceVideoPath, UpdateSourceTrigger=PropertyChanged}" />
            <Button Grid.Column="1" Command="{Binding SelectDestinationVideoCommand}">
                <materialDesign:PackIcon Kind="FolderOpen" />
            </Button>
        </Grid>
    </Grid>
</UserControl>